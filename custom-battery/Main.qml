import QtQuick
import Quickshell

// Main entry point for the plugin (optional background logic)
Item {
    // Properties and signals can be defined here if needed

    id: root
    property var pluginApi: null
}
